# Empty URL = Axios uses relative paths; Vite proxies /threat-indicators + /ai to the JVM backend.
# Phones can use http://YOUR_PC_IP:5173 — they never need to reach localhost:8080 directly.
VITE_API_BASE_URL=
